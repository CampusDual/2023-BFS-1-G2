import { HttpHeaders } from '@angular/common/http';
import { Injector } from '@angular/core';
import { Observable } from 'rxjs';
import { IExportDataProvider } from '../../interfaces/export-data-provider.interface';
import { IExportService } from '../../interfaces/export-service.interface';
import { OntimizeBaseService } from './ontimize-base-service.class';
export declare class OntimizeExportService3X extends OntimizeBaseService implements IExportService {
    protected injector: Injector;
    exportPath: string;
    servicePath: string;
    protected exportDataProvider: IExportDataProvider;
    constructor(injector: Injector);
    configureService(config: any): void;
    protected buildHeaders(): HttpHeaders;
    exportData(format: string): Observable<any>;
}
